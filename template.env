# Airflow configuration - set to project's airflow directory
# IMPORTANT: Use the full absolute path (e.g., /Users/you/TypeWiki/src/typewiki/airflow)
AIRFLOW_HOME=/path/to/TypeWiki/src/typewiki/airflow

# Airflow JWT Secret (required for Docker, optional for local make commands)
# Generate with: openssl rand -hex 32
# Note: The Makefile provides a default for local development
AIRFLOW_API_AUTH_JWT_SECRET=

# OpenAI API configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL_NAME=gpt-4o-mini

# Embedding model for RAG (text-embedding-3-large produces 3072 dimensions)
OPENAI_EMBEDDING_MODEL_NAME=text-embedding-3-large

# Pinecone API Configuration (required for vector storage)
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX_NAME=typewiki-helpcenter

# HTTP client timeout for LLM requests (in seconds)
HTTP_CLIENT_TIMEOUT_SECONDS=300

# PDF directory for Help Center articles (optional, defaults to project pdfs/)
# TYPEWIKI_PDF_DIR=/path/to/pdfs
